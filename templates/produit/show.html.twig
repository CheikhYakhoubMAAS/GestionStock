{% extends 'template.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <div class="container">
      <div class="panel panel-primary">
        <div class=" panel-heading">
         <i class="icon_menu"></i>Produits
         </div>
          <div class="panel-body">
  

    <table class="table table-condensed">
      
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ produit.libelle }}</td>
            </tr>
            <tr>
                <th>Pa</th>
                <td>{{ produit.pa }}</td>
            </tr>
            <tr>
                <th>Pv</th>
                <td>{{ produit.pv }}</td>
            </tr>
            <tr>
                <th>Tva</th>
                <td>{{ produit.tva }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ produit.stock }}</td>
            </tr>
            
        </tbody>
    </table>

    <a href="{{ path('produit_index') }}"class="btn btn-warning">back to list</a>

    <a href="{{ path('produit_edit', {'id': produit.id}) }}"class="btn btn-success">edit</a>

    {{ include('produit/_delete_form.html.twig') }}
</div>
</div>
</div>
{% endblock %}
